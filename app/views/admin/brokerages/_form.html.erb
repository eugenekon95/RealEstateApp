<%= form_with model: [:admin, brokerage] do |form| %>
  <% if brokerage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brokerage.errors.count, "error") %> prohibited this user from being saved:</h2>
      <ul>
        <% brokerage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-label-group mb-3">
    <%= form.label :name%>
    <%= form.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-label-group mb-3">
    <%= form.label :city%>
    <%= form.text_field :city, class: 'form-control' %>
  </div>
  <div class="form-label-group mb-3">
    <%= form.label :address%>
    <%= form.text_field :address, class: 'form-control' %>
  </div>
  <div class="actions ">
    <%= form.submit nil, class: "btn btn-success" %>
  </div>
<% end %>
